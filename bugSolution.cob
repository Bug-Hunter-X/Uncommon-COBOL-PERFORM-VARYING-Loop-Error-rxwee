MOVE ZERO TO WS-COUNTER.
MOVE ZERO TO WS-EXIT-FLAG.
PERFORM VARYING WS-INDEX FROM 1 BY 1 UNTIL WS-INDEX > 100 OR WS-EXIT-FLAG = 1
    ADD 1 TO WS-COUNTER
    IF WS-COUNTER > 50 THEN
        MOVE 1 TO WS-EXIT-FLAG
    END-IF
END-PERFORM.
DISPLAY "Counter value: " WS-COUNTER.